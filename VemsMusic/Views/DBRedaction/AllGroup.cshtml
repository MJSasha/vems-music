@model AllGroupAndAllGenreViewModel

<div class="container">
    <div class="row">
        <ul class="list-group">

            <!-- Список ID жанров -->
            <div class="btn-group dropend mt-2 mb-3">
                <button type="button" class="btn btn-secondary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                    Список ID жанров
                </button>

                <ul class="dropdown-menu">
                    @foreach (var item in Model.AllGenres)
                    {
                        <li><a class="dropdown-item" href="#">@item.Id - @item.Name</a></li>
                    }
                </ul>
            </div>

            <p>Редактирование информации о группе/исполнителе</p>
            @foreach (var item in Model.AllGroups)
            {
                <div class="accordion-item">
                    <h2 class="accordion-header" id="heading-@item.Id">
                        <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapse-@item.Id" aria-expanded="true" aria-controls="collapseOne">
                            @item.Name
                            <button class="delete-genre"><a href="~/DBRedaction/DeleteGroup/@item.Id">Удалить</a></button>
                        </button>
                    </h2>
                    <div id="collapse-@item.Id" class="accordion-collapse collapse" aria-labelledby="heading-@item.Id" data-bs-parent="#accordionExample">
                        @await Html.PartialAsync("GroupRedaction", item);
                    </div>
                </div>
            }
        </ul>
    </div>
</div>