@model MusicViewModel

<div class="container">
    <div class="row">

        @foreach (Music item in Model.AllMusic)
        {
            @await Html.PartialAsync("OneMusicOnScreen", item)
        }

    </div>
</div>